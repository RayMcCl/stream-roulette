/* --- Resources --- */
import './Stream.style.scss';

/** Component */
import * as React from 'react';
import { Props } from './Stream.types';

/** Modules */


/** Stream [ClassDescription] */
const Stream : React.FunctionComponent<Props> = (props) => (
	<div className="stream">
		
	</div>
);

export default Stream;