import * as React from 'react';
import {{pascalCase name}} from './{{pascalCase name}}.component';

export default function (this: {{pascalCase name}}) {
	return (
		<div className="{{kebabCase name}}">
			
		</div>
	);
}