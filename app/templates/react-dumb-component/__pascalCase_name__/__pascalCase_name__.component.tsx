/* --- Resources --- */
import './{{pascalCase name}}.style.scss';

/** Component */
import * as React from 'react';
import { Props } from './{{pascalCase name}}.types';

/** Modules */


/** {{pascalCase name}} [ClassDescription] */
const {{pascalCase name}} : React.FunctionComponent<Props> = (props) => (
	<div className="{{kebabCase name}}">
		
	</div>
);

export default {{pascalCase name}};